apiVersion: v1
kind: Service
metadata:
  name: django-app-service
  labels:
    app: django-app
spec:
  selector:
    app: django-app
    version: v1        # initially points to blue deployment
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000

